# SAVIS Executor

**SAVIS Executor** is the Python execution service of [SAVIS](../README.md). It is used by the [SAVIS API](../savis-api/README.md) to collect provider offers for an item through asynchronous jobs and provider adapters.
